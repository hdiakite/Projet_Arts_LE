import React, { useState } from 'react';
import './style.css';
import {
  Form,
  Input,
  Button,
  Select,
  InputNumber,
  Typography,
  Space,
  Tag,
} from 'antd';
import { MinusCircleOutlined, PlusOutlined } from '@ant-design/icons';

const layout = {
  labelCol: { span: 30 },
  wrapperCol: { span: 30 },
};

const { Title } = Typography;

const ComponentContainedRequest = () => {
  return <div></div>;
};

export default ComponentContainedRequest;
